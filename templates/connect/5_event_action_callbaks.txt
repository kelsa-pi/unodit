
    def ${CONTROL_NAME}_OnClick(self):
        self.DialogContainer.Title = "It's Alive! - ${CONTROL_NAME}"
        self.messageBox("It's Alive! - ${CONTROL_NAME}", "Event: OnClick", INFOBOX)
        # TODO: not implemented
